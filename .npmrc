# Angular 18's build-angular declares an optional peer on tailwindcss ^2||^3.
# We run Tailwind v4 through PostCSS (.postcssrc.json) instead of Angular's
# built-in Tailwind detection, so that range does not apply to us.
legacy-peer-deps=true
